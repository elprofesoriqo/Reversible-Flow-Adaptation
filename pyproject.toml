[project]
name = "robociki"
version = "0.1.0"
readme = "research_proposal.md"
requires-python = ">=3.14"
dependencies = [
    "jax[cuda12]",
    "flax",
    "optax",
    "mujoco",
    "mujoco-mjx",
    "einops",
    "tqdm",
    "wandb",
    "numpy",
]

[tool.pyright]
extraPaths = ["."]
